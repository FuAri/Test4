<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Mapper.ClassMapper">
    <select id="findClassById" resultType="class" parameterType="Integer">
        select * from tb_class where class_id = #{classId}
    </select>

<!--    添加一个系-->
    <insert id="addClass" parameterType="class">
        insert into tb_class (class_id,class_name,class_size,class_surplus,dept_id,major_id,tutor_id,instructor_id,bz)
        values (#{classId},#{className},#{classSize},#{classSurplus},#{deptId},#{majorId},#{tutorId},#{instructorId},#{bz})
    </insert>
    <update id="updateClass" parameterType="class">
        update tb_class set class_name=#{className},class_size=#{classSize},class_surplus=#{classSurplus},dept_id=#{deptId}
         ,major_id=#{majorId},tutor_id=#{tutorId},instructor_id=#{instructorId},bz=#{bz}
         where class_id=#{classId}
    </update>
    <delete id="deleteClass" parameterType="Integer">
        delete  from tb_class where class_id=#{classId}
    </delete>
</mapper>

